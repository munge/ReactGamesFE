import type { GameAssetBundle } from '@common/pixi/PixiAssetsLoader'

/**
 * Game 2 asset bundle.
 * Slot Spinner reel textures, etc.
 */
export const GAME2_BUNDLE: GameAssetBundle = {
  name: 'game-2',
  assets: [
    // { alias: 'reel-bg',    src: `${import.meta.env.VITE_ASSET_URL}/game-2/reel-bg.png` },
    // { alias: 'symbols',    src: `${import.meta.env.VITE_ASSET_URL}/game-2/symbols.json` },
  ],
}
